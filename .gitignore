node_modules/
dist/
dev-data/
target/
package-lock.json

# 旧的 /shop/ 托管目录（架构已改为用户端网页内嵌 exe，/shop/ 已废弃）
web/shop/

# Vite 构建分析产物（bundle-size 分析器生成的，非源码）
admin/bundle-stats.html

# 本项目目录的 Claude Code 本地配置（权限白名单等，各机器各不相同）
.claude/
