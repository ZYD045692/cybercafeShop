# cybercafeShop 使用教程

网吧/电竞馆自助点购系统：顾客在客户机上自己选商品、扫码付款，吧台主机立刻弹窗 + 语音播报提醒出货，手机上还能扫码随时加新商品。

全程不需要联网（纯局域网运行），数据都存在吧台主机上。

## 这套东西由三部分组成

| 部分 | 装在哪 | 干什么 |
|---|---|---|
| **管理端** | 吧台主机（装一次） | 管商品、接订单、看流水、改设置，同时它也是对内的小服务器 |
| **用户端** | 每台客户机（拷过去就能用） | 顾客点购界面 |
| **手机端** | 不用装，扫码即用 | 用手机拍照快速录入新商品 |

---

## 一、部署

### 1. 吧台主机：安装管理端

运行 `cybercafeShop-admin_v1.0.0_setup.exe`，按向导装完（装在当前用户目录，不需要管理员权限）。

![安装向导](images/01_安装向导.png)

装完启动一次，它会在程序旁边自动生成这些东西（**不用手动建**）：

```
管理端.exe 旁边
├─ config.ini      端口配置（默认 21974，一般不用动）
├─ data\           所有数据（商品、订单、图片、收款码）
└─ web\            手机端页面
```

> 第一次启动 Windows 可能弹防火墙提示，**勾上「专用网络」允许**，否则客户机和手机连不进来。

### 2. 客户机：部署用户端

把 `用户端` 文件夹整个拷到每台客户机，打开里面的 `config.ini`，只改一行——把 `host` 改成**吧台主机的局域网 IP**：

```ini
[server]
host = 192.168.1.100    ← 改成吧台主机的 IP
port = 21974            ← 和吧台主机的 config.ini 保持一致
contact = 13800000000   ← 网管电话，连不上时会显示给顾客
```

![改 config.ini](images/02_修改用户端配置.png)

改完双击 `莱尚网电竞馆点购.exe` 就能用。连不上吧台时会直接弹提示框告诉顾客去吧台，不会卡在一个坏页面上：

![连不上提示](images/03_连不上提示.png)

> 吧台主机的 IP 怎么看：在吧台主机上按 `Win+R` 输入 `cmd` 回车，敲 `ipconfig`，找「IPv4 地址」。建议把吧台主机的 IP 在路由器里固定住，免得重启路由后变了。

---

## 二、管理端日常使用

### 接订单（订单管理）

顾客一下单，这里立刻出现一条「未处理」，**右下角同时弹提醒卡片 + 语音播报**「电竞区08 购买商品」。出货后点「处理」即可。

![订单管理](images/04_订单管理.png)

![订单提醒卡片](images/05_订单提醒卡片.png)

### 商品管理

商品列表一目了然：图、名称、进价、售价、上下架开关；右上角能按名称或缩拼搜索（比如输入 `bskl` 找百事可乐）。蓝色开关 = 正在销售，关掉顾客就看不到了。

![商品管理](images/06_商品管理.png)

**改商品**：点商品图片（悬停会显示「✏️ 点击编辑」）就能改图、改名、改价、删除。编辑时缩拼是锁定的，避免图片名对不上。

![编辑商品](images/07_编辑商品.png)

**分类管理**：改名、删除、加新分类都在这一个弹窗里。注意：下面还挂着商品的分类不让删，先把商品挪走。

![分类管理](images/08_分类管理.png)

### 加商品（电脑上）

点右上角「添加商品」，对话框右边有个**二维码**——那是给手机用的（见下一节）。在电脑上直接加的话：

![添加商品对话框](images/09_添加商品对话框.png)

1. 点图片区域，选一张商品照片
2. **系统会自动把背景抠掉**，只留商品主体。抠完可以用滑块转正、缩放、翻转，满意了点「应用」

![图片编辑器](images/10_图片编辑器.png)

3. 填名称（**缩拼会自动生成**，就是顾客搜索时用的拼音首字母，一般不用改）、分类、进价售价，保存

![填写商品信息](images/11_填写商品信息.png)

保存后立刻出现在列表里：

![商品列表](images/12_商品列表.png)

> 缩拼撞了会自动加后缀（whh → whh_1），不用操心。图片没传上去时商品也会保存（会提示「已保存但图传失败」），可稍后点进商品重新编辑补图。

### 用手机加商品（推荐，拍照更方便）

手机连上**店里的 WiFi**（必须和吧台主机同一个网），扫「添加商品」对话框里的二维码，直接打开录入页面：

![手机端](images/13_手机端.png)

拍照或从相册选图 → 同样自动抠图 → 可以转正、缩放、翻转：

![手机端编辑器](images/14_手机端编辑器.png)

填名称价格保存，提示「商品已保存」（图片没传上去时会提示「已保存但图传失败」，商品也已经保存，可稍后在管理端重新编辑补图），顾客端马上就能看到它了。
吧台要加新商品时，拿着手机对着商品拍一圈就录好了：

![手机端保存成功](images/15_手机端保存成功.png)

> 抠图第一次用要加载模型（约 18MB），之后手机会缓存住，再用就是秒开。管理端和手机端共用同一份模型，不占双份空间。

**如果手机打开提示「无法连接主机。」**：说明手机连不到吧台机——查 WiFi 是不是店里的、吧台机管理端是否开着。

![手机端连不上](images/16_手机端连不上.png)

### 看流水（销售记录）

按日期、支付方式筛选，右上角直接给合计金额：

![销售记录](images/17_销售记录.png)

### 设置

![设置](images/18_设置.png)

- **店铺信息**：店名和欢迎语，显示在用户端顶部
- **收款码**：点图片换成你自己的微信/支付宝收款码（顾客扫码付的就是这里配的码）
- **开机自动启动**：建议开，吧台机重启后不用管
- **语音播报 / 订单提醒**：各有一个测试按钮，装完点一下确认好使

---

## 三、顾客那边看到的（用户端）

打开就是商品墙，顶部是店名和欢迎语：

![用户端选购](images/19_用户端选购.png)

顶上的搜索框支持**拼音首字母**——输 `bskl` 百事可乐全出来了：

![用户端搜索](images/20_用户端搜索.png)

点商品加入购物车，右侧随时改数量：

![用户端购物车](images/21_用户端购物车.png)

去结算 → 确认清单 → 选支付方式：

![用户端确认订单](images/22_用户端确认订单.png)

**微信/支付宝**：扫收款码付款后点「我已付款，提交订单」，吧台那边立刻弹窗播报：

![用户端扫码付款](images/23_用户端扫码付款.png)

**现金**：不用扫码，提交后提示准备好现金、吧台稍后送货：

![用户端现金支付](images/24_用户端现金支付.png)

右上角「呼叫网管」：点一下，吧台会听到语音播报并看到是哪台机器在叫：

![用户端呼叫网管](images/25_用户端呼叫网管.png)

---

## 四、数据在哪、怎么备份

所有数据都在吧台主机 `管理端.exe` 旁边的 `data\` 文件夹里：

| 位置 | 内容 |
|---|---|
| `data\db\shop_db.db` | 商品库 + 订单记录 |
| `data\db\config.db` | 店名、欢迎语 |
| `data\image\` | 商品图片（文件名 = 缩拼） |
| `data\qrcode\` | 收款码 |
| `config.ini` | 端口 |

**备份 = 复制 `data` 文件夹**，恢复 = 放回去覆盖。

如果拿到一个「商品数据包」（里面是 `data\db` + `data\image`），退出管理端，把包里的 `data` 解压到 exe 同级选覆盖，再启动即可整套导入。

---

## 五、常见问题

**客户机打开提示连不上？**
按顺序查：① 客户机 `config.ini` 的 `host` 是不是吧台机当前 IP；② 管理端是不是开着；③ 吧台机防火墙有没有放行（专用网络）；④ 两端 `port` 是否一致。

**手机扫码打不开页面？**
手机必须连店里 WiFi（和手机流量/别的网络不通）。如果二维码里的 IP 看着就不对（比如 169.254 开头），是吧台机有虚拟网卡/代理软件干扰，把不用的虚拟网卡禁用再重开对话框。

**语音播报没声？**
先看设置页「试听播报效果」有没有声，没有就查系统音量和默认播放设备。

**想换端口？**
改吧台机 `config.ini` 里的 `port`，重启管理端；每台客户机的 `config.ini` 也改成同一个。端口别用常见端口，默认 21974 就挺好。

**杀毒软件报毒？**
程序是本地应用加内嵌网页的形态，容易被误报，加白名单即可。
