商品库种子（生产）。

这里的 shop_db.db 是空库：只有表结构和系统分类「全部商品」，没有任何商品。
发布不带商品数据——商品库和图片在独立的「商品数据包」里，
部署时把数据包的 data\ 覆盖到安装目录的 data\ 即可（详见数据包里的 使用说明.txt）。
日常商品增删改在管理端「商品管理」界面完成，不需要换库。

说明：店铺配置（店名/欢迎语）运行时自动生成在同目录的 config.db，不在种子里。
